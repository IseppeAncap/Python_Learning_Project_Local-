🧠 Entendendo o código de ordenação de dicionários

🧩 1. Estrutura uma lista composta pro  dicionários.

{'nome': 'Luiz', 'sobrenome': 'miranda'}

⚙️ 2. a função ordena 
  

    ordena(item)
        return item ['nome'] or item['sobrenome']

    Cada chamada da func 

    retorna um item da chave item[sobrenome] da pessoa

    Exemplo 
    {'nome': 'Luiz', 'sobrenome': 'miranda'},

    # retorna : miranda

   🔁 3 - O método sorted() ou sort()

   Esses funções chamam varias vezes a func ordena -  uma vez para cada item na lista o valores retornados extraidos (nome ; sobrenome) seram usados pro sorted reorganizados em ordem alfabetica 
    
    4 - dica sort cria uma copia raza


        
